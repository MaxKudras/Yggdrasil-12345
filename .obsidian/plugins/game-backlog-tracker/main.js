/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// main.ts
var main_exports = {};
__export(main_exports, {
  default: () => GameBacklogPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian = require("obsidian");

// i18n.ts
var esTranslations = {
  commandInsertBlock: "Insertar bloque game-backlog",
  commandInsertProfileBlock: "Insertar bloque game-backlog-profile",
  renderError: "Error renderizando game-backlog. Revisa la consola de desarrollador.",
  profileLoading: "Analizando bloques game-backlog...",
  profileDefaultPlayer: "Jugador",
  profileAnalyzedFiles: "Archivos analizados: {count}",
  profileFilesTitle: "Archivos con bloques game-backlog",
  profileRefreshButton: "\u{1F504} Recalcular",
  profileRefreshTooltip: "Volver a analizar todos los bloques game-backlog",
  profileNoGames: "No se encontraron juegos en bloques game-backlog.",
  profileStatCompleted: "Total juegos completados",
  profileStatHours: "Total de horas jugadas",
  profileStatPlatinums: "Platinos",
  profileStatMostUsedPlatform: "Consola mas usada",
  profileRetroAchievements: "RetroAchievements",
  downloadErrorPrefix: "Error descargando imagen: {error}",
  downloadErrorUnavailable: "No se pudo descargar la imagen. El servidor puede tener restricciones CORS o estar caido.",
  lockTitleLocked: "Desbloquear edicion",
  lockTitleUnlocked: "Bloquear edicion",
  toggleViewTitle: "Alternar entre vista de tarjetas y tabla",
  noName: "Sin nombre",
  platformBrandAlt: "Platform brand",
  platinumBadgeAlt: "Platinum badge",
  tableHeaderGame: "Juego",
  tableHeaderPlatform: "Plataforma",
  tableHeaderDate: "Fecha",
  tableHeaderRating: "Puntaje",
  tableHeaderDuration: "Duracion",
  emptyValue: "\u2014",
  statsCompleted: "Juegos completados: {count}",
  statsDlcCompleted: "DLCs completados: {count}",
  statsTotalHours: "Horas totales: {hours}~ hs",
  topGameSelectBest: "Seleccionar mejor juego...",
  topGameSelectSecond: "Seleccionar segundo mejor juego...",
  topGameSelectThird: "Seleccionar tercer juego...",
  noticeGameAdded: "Juego agregado al backlog",
  noticeGameUpdated: "Juego actualizado",
  noticeEntryNotFound: "No se pudo encontrar la entrada a editar.",
  modalAddTitle: "Agregar Nuevo Juego",
  modalEditTitle: "Editar Juego",
  modalNameLabel: "Nombre del juego",
  modalNameDesc: "Titulo del videojuego",
  modalNamePlaceholder: "Ej: The Legend of Zelda",
  modalCoverLabel: "URL de la portada",
  modalCoverDesc: "Link a la imagen de la portada",
  modalCoverPlaceholder: "https://ejemplo.com/portada.jpg",
  modalCardColorLabel: "Color de la tarjeta",
  modalCardColorDesc: "Color de fondo para la parte inferior de la tarjeta",
  modalCardColorPlaceholder: "#35393d",
  modalTextColorLabel: "Color del texto",
  modalTextColorDesc: "Color del texto en la parte inferior de la tarjeta",
  modalTextColorPlaceholder: "#f9fafb",
  modalNoImageBackgroundColorLabel: "Color de fondo sin imagen",
  modalNoImageBackgroundColorDesc: "Color de fondo cuando el juego no tiene portada",
  modalNoImageBackgroundColorPlaceholder: "#334155",
  modalNoImageTextColorLabel: "Color de texto sin imagen",
  modalNoImageTextColorDesc: "Color del titulo cuando el juego no tiene portada",
  modalNoImageTextColorPlaceholder: "#ffffff",
  modalDownloadTooltip: "Descargar imagen remota a carpeta configurada",
  noticeNoActiveFile: "No hay archivo abierto",
  noticeImageDownloaded: "Imagen descargada exitosamente",
  modalRatingLabel: "Puntuacion",
  modalRatingDesc: "Se ajusta al tipo de puntuacion configurado en el plugin",
  modalCompletionDateLabel: "Fecha de completacion",
  modalCompletionDateDesc: "Fecha en que terminaste el juego",
  modalPlatformLabel: "Plataforma",
  modalPlatformDesc: "Consola o plataforma donde lo jugaste",
  modalHoursLabel: "Horas jugadas",
  modalHoursDesc: "Cantidad de horas (acepta decimales, ej: 2.5)",
  modalPlatinumLabel: "Platinado",
  modalPlatinumDesc: "Logro de platino obtenido",
  modalDlcLabel: "DLC / Expansi\xF3n",
  modalDlcDesc: "Es una campa\xF1a o contenido descargable",
  dlcBadgeAlt: "DLC",
  buttonSave: "Guardar",
  buttonAdd: "Agregar",
  buttonCancel: "Cancelar",
  buttonEdit: "Editar",
  buttonDelete: "Eliminar",
  confirmDeleteGame: "Estas seguro de que quieres eliminar este juego?",
  noticeGameDeleted: "Juego eliminado",
  viewRatingLabel: "Puntuacion: ",
  viewPlatformLabel: "Plataforma: ",
  viewCompletedLabel: "Completado: ",
  viewHoursLabel: "Horas jugadas: ",
  viewStatusLabel: "Estado: ",
  viewStatusPlatinum: "\u{1F3C6} Platinado",
  settingsTitle: "Configuracion de Game Backlog",
  settingsLanguageName: "Idioma",
  settingsLanguageDesc: "Idioma de la interfaz del plugin",
  languageSpanish: "Espanol",
  languageEnglish: "Ingles",
  settingsDefaultCoverName: "Imagen de portada por defecto",
  settingsDefaultCoverDesc: "URL de la imagen a mostrar cuando no se especifica portada",
  settingsDefaultCoverPlaceholder: "https://ejemplo.com/default.jpg",
  settingsDefaultPlatformName: "Plataforma por defecto",
  settingsDefaultPlatformDesc: "Plataforma usada por defecto al agregar juegos y cuando no se puede leer una plataforma valida",
  settingsPlatformModeName: "Platform mode",
  settingsPlatformModeDesc: "None: no muestra plataforma, Label: etiqueta con icono y nombre",
  platformModeNone: "None",
  platformModeLabel: "Label",
  settingsScoreTypeName: "Tipo de puntuacion",
  settingsScoreTypeDesc: "Define como se muestra y edita la puntuacion en todas las vistas del plugin",
  scoreTypeStars5: "1 - 5 estrellas",
  scoreTypeStars5Half: "Puntuacion de media estrella",
  scoreTypeStars10: "1 - 10 estrellas",
  scoreTypeNumeric10: "0 - 10 numerico",
  settingsDownloadFolderName: "Carpeta de descarga de imagenes",
  settingsDownloadFolderDesc: "Carpeta en el vault donde se descargaran las imagenes remotas. Si esta vacia, se usara la carpeta del archivo",
  settingsDownloadFolderPlaceholder: "attachments",
  settingsCardColorName: "Color de la tarjeta",
  settingsCardColorDesc: "Color de fondo para la parte inferior de todas las tarjetas",
  settingsCardColorPlaceholder: "#35393d",
  settingsTextColorName: "Color del texto",
  settingsTextColorDesc: "Color del texto en la parte inferior de todas las tarjetas",
  settingsTextColorPlaceholder: "#f9fafb",
  settingsNoImageBackgroundColorName: "Color de fondo sin imagen",
  settingsNoImageBackgroundColorDesc: "Color de fondo cuando una tarjeta no tiene portada",
  settingsNoImageBackgroundColorPlaceholder: "#334155",
  settingsNoImageTextColorName: "Color de texto sin imagen",
  settingsNoImageTextColorDesc: "Color del titulo cuando una tarjeta no tiene portada",
  settingsNoImageTextColorPlaceholder: "#ffffff",
  settingsPluginAssetsName: "Assets del plugin",
  settingsPluginAssetsDesc: "Crea la carpeta assets del plugin y descarga todos los archivos del repositorio oficial",
  settingsPluginAssetsButton: "Descargar assets oficiales",
  settingsPluginAssetsRunning: "Descargando...",
  noticeAssetsSyncUnsupported: "Tu adaptador de vault no permite escribir archivos binarios.",
  noticeAssetsSyncStarted: "Descargando assets del plugin...",
  noticeAssetsSyncNoFiles: "No se encontraron archivos para descargar.",
  noticeAssetsSyncSuccess: "Assets descargados correctamente: {count}",
  noticeAssetsSyncError: "Error al descargar assets: {error}",
  settingsSteamGridDbName: "SteamGridDB API Key",
  settingsSteamGridDbDesc: "API key para buscar portadas en SteamGridDB. Gener\xE1 una en steamgriddb.com > Preferencias",
  settingsSteamGridDbPlaceholder: "Tu API key de SteamGridDB",
  steamGridDbSearchTooltip: "Buscar portada en SteamGridDB",
  steamGridDbNoApiKey: "Configur\xE1 tu API key de SteamGridDB en las opciones del plugin primero",
  steamGridDbModalTitle: "Buscar en SteamGridDB",
  steamGridDbSearchPlaceholder: "Nombre del juego...",
  steamGridDbSearchAction: "Buscar",
  steamGridDbBackButton: "\u2190 Volver",
  steamGridDbNoResults: "No se encontraron juegos",
  steamGridDbLoadingGames: "Buscando juegos...",
  steamGridDbLoadingGrids: "Cargando portadas...",
  steamGridDbNoGrids: "No se encontraron portadas para este juego",
  steamGridDbSearchError: "Error al buscar: {error}"
};
var enTranslations = {
  commandInsertBlock: "Insert game-backlog block",
  commandInsertProfileBlock: "Insert game-backlog-profile block",
  renderError: "Error rendering game-backlog. Check the developer console.",
  profileLoading: "Analyzing game-backlog blocks...",
  profileDefaultPlayer: "Player",
  profileAnalyzedFiles: "Analyzed files: {count}",
  profileFilesTitle: "Files with game-backlog blocks",
  profileRefreshButton: "\u{1F504} Recalculate",
  profileRefreshTooltip: "Analyze all game-backlog blocks again",
  profileNoGames: "No games were found in game-backlog blocks.",
  profileStatCompleted: "Completed games",
  profileStatHours: "Total played hours",
  profileStatPlatinums: "Platinums",
  profileStatMostUsedPlatform: "Most used platform",
  profileRetroAchievements: "RetroAchievements",
  downloadErrorPrefix: "Error downloading image: {error}",
  downloadErrorUnavailable: "Could not download image. The server may have CORS restrictions or be down.",
  lockTitleLocked: "Unlock editing",
  lockTitleUnlocked: "Lock editing",
  toggleViewTitle: "Toggle between card and table views",
  noName: "Untitled",
  platformBrandAlt: "Platform brand",
  platinumBadgeAlt: "Platinum badge",
  tableHeaderGame: "Game",
  tableHeaderPlatform: "Platform",
  tableHeaderDate: "Date",
  tableHeaderRating: "Rating",
  tableHeaderDuration: "Duration",
  emptyValue: "\u2014",
  statsCompleted: "Completed games: {count}",
  statsDlcCompleted: "Completed DLCs: {count}",
  statsTotalHours: "Total hours: {hours}~ hrs",
  topGameSelectBest: "Select best game...",
  topGameSelectSecond: "Select second best game...",
  topGameSelectThird: "Select third game...",
  noticeGameAdded: "Game added to backlog",
  noticeGameUpdated: "Game updated",
  noticeEntryNotFound: "Could not find the entry to edit.",
  modalAddTitle: "Add New Game",
  modalEditTitle: "Edit Game",
  modalNameLabel: "Game name",
  modalNameDesc: "Video game title",
  modalNamePlaceholder: "Ex: The Legend of Zelda",
  modalCoverLabel: "Cover URL",
  modalCoverDesc: "Link to the cover image",
  modalCoverPlaceholder: "https://example.com/cover.jpg",
  modalCardColorLabel: "Card color",
  modalCardColorDesc: "Background color for the lower part of the card",
  modalCardColorPlaceholder: "#35393d",
  modalTextColorLabel: "Text color",
  modalTextColorDesc: "Text color for the lower part of the card",
  modalTextColorPlaceholder: "#f9fafb",
  modalNoImageBackgroundColorLabel: "No image background color",
  modalNoImageBackgroundColorDesc: "Background color used when the game has no cover image",
  modalNoImageBackgroundColorPlaceholder: "#334155",
  modalNoImageTextColorLabel: "No image text color",
  modalNoImageTextColorDesc: "Title color used when the game has no cover image",
  modalNoImageTextColorPlaceholder: "#ffffff",
  modalDownloadTooltip: "Download remote image to configured folder",
  noticeNoActiveFile: "No active file",
  noticeImageDownloaded: "Image downloaded successfully",
  modalRatingLabel: "Rating",
  modalRatingDesc: "Uses the score type configured in the plugin settings",
  modalCompletionDateLabel: "Completion date",
  modalCompletionDateDesc: "Date when you finished the game",
  modalPlatformLabel: "Platform",
  modalPlatformDesc: "Console or platform where you played it",
  modalHoursLabel: "Played hours",
  modalHoursDesc: "Hours amount (supports decimals, e.g.: 2.5)",
  modalPlatinumLabel: "Platinum",
  modalPlatinumDesc: "Platinum achievement obtained",
  modalDlcLabel: "DLC / Expansion",
  modalDlcDesc: "Is a DLC campaign or downloadable content",
  dlcBadgeAlt: "DLC",
  buttonSave: "Save",
  buttonAdd: "Add",
  buttonCancel: "Cancel",
  buttonEdit: "Edit",
  buttonDelete: "Delete",
  confirmDeleteGame: "Are you sure you want to delete this game?",
  noticeGameDeleted: "Game deleted",
  viewRatingLabel: "Rating: ",
  viewPlatformLabel: "Platform: ",
  viewCompletedLabel: "Completed: ",
  viewHoursLabel: "Played hours: ",
  viewStatusLabel: "Status: ",
  viewStatusPlatinum: "\u{1F3C6} Platinum",
  settingsTitle: "Game Backlog Settings",
  settingsLanguageName: "Language",
  settingsLanguageDesc: "Plugin interface language",
  languageSpanish: "Spanish",
  languageEnglish: "English",
  settingsDefaultCoverName: "Default cover image",
  settingsDefaultCoverDesc: "Image URL shown when no cover is provided",
  settingsDefaultCoverPlaceholder: "https://example.com/default.jpg",
  settingsDefaultPlatformName: "Default platform",
  settingsDefaultPlatformDesc: "Platform used by default when adding games and when a valid platform cannot be parsed",
  settingsPlatformModeName: "Platform mode",
  settingsPlatformModeDesc: "None: hide platform, Label: tag with icon and platform name",
  platformModeNone: "None",
  platformModeLabel: "Label",
  settingsScoreTypeName: "Score type",
  settingsScoreTypeDesc: "Defines how scores are edited and displayed across the plugin",
  scoreTypeStars5: "1 - 5 stars",
  scoreTypeStars5Half: "Half star rating",
  scoreTypeStars10: "1 - 10 stars",
  scoreTypeNumeric10: "0 - 10 numeric",
  settingsDownloadFolderName: "Image download folder",
  settingsDownloadFolderDesc: "Vault folder where remote images will be downloaded. If empty, the current markdown file folder is used",
  settingsDownloadFolderPlaceholder: "attachments",
  settingsCardColorName: "Card color",
  settingsCardColorDesc: "Background color for the lower part of all cards",
  settingsCardColorPlaceholder: "#35393d",
  settingsTextColorName: "Text color",
  settingsTextColorDesc: "Text color for the lower part of all cards",
  settingsTextColorPlaceholder: "#f9fafb",
  settingsNoImageBackgroundColorName: "No image background color",
  settingsNoImageBackgroundColorDesc: "Background color used when a card has no cover image",
  settingsNoImageBackgroundColorPlaceholder: "#334155",
  settingsNoImageTextColorName: "No image text color",
  settingsNoImageTextColorDesc: "Title color used when a card has no cover image",
  settingsNoImageTextColorPlaceholder: "#ffffff",
  settingsPluginAssetsName: "Plugin assets",
  settingsPluginAssetsDesc: "Create the plugin assets folder and download all files from the official repository",
  settingsPluginAssetsButton: "Download official assets",
  settingsPluginAssetsRunning: "Downloading...",
  noticeAssetsSyncUnsupported: "Your vault adapter does not support binary file writes.",
  noticeAssetsSyncStarted: "Downloading plugin assets...",
  noticeAssetsSyncNoFiles: "No files were found to download.",
  noticeAssetsSyncSuccess: "Assets downloaded successfully: {count}",
  noticeAssetsSyncError: "Error downloading assets: {error}",
  settingsSteamGridDbName: "SteamGridDB API Key",
  settingsSteamGridDbDesc: "API key for searching covers on SteamGridDB. Generate one at steamgriddb.com > Preferences",
  settingsSteamGridDbPlaceholder: "Your SteamGridDB API key",
  steamGridDbSearchTooltip: "Search cover on SteamGridDB",
  steamGridDbNoApiKey: "Configure your SteamGridDB API key in plugin settings first",
  steamGridDbModalTitle: "Search on SteamGridDB",
  steamGridDbSearchPlaceholder: "Game name...",
  steamGridDbSearchAction: "Search",
  steamGridDbBackButton: "\u2190 Back",
  steamGridDbNoResults: "No games found",
  steamGridDbLoadingGames: "Searching games...",
  steamGridDbLoadingGrids: "Loading covers...",
  steamGridDbNoGrids: "No covers found for this game",
  steamGridDbSearchError: "Search error: {error}"
};
var translations = {
  es: esTranslations,
  en: enTranslations
};
function translate(language, key, vars) {
  var _a, _b;
  const table = (_a = translations[language]) != null ? _a : esTranslations;
  let text = (_b = table[key]) != null ? _b : esTranslations[key];
  if (!vars)
    return text;
  return text.replace(/\{(\w+)\}/g, (_, varName) => {
    var _a2;
    return String((_a2 = vars[varName]) != null ? _a2 : "");
  });
}

// main.ts
var GamePlatform = /* @__PURE__ */ ((GamePlatform2) => {
  GamePlatform2["SWITCH"] = "Switch";
  GamePlatform2["PC"] = "PC";
  GamePlatform2["STEAM_DECK"] = "Steam Deck";
  GamePlatform2["PS_VITA"] = "Playstation Vita";
  GamePlatform2["PS1"] = "Playstation 1";
  GamePlatform2["PS2"] = "Playstation 2";
  GamePlatform2["PS3"] = "Playstation 3";
  GamePlatform2["PS4"] = "Playstation 4";
  GamePlatform2["PS5"] = "Playstation 5";
  GamePlatform2["NINTENDO_3DS"] = "3DS";
  GamePlatform2["NINTENDO_DS"] = "DS";
  GamePlatform2["GBA"] = "Game Boy Advance";
  GamePlatform2["DREAMCAST"] = "Dreamcast";
  GamePlatform2["PSP"] = "Playstation Portable";
  GamePlatform2["GAMECUBE"] = "GameCube";
  GamePlatform2["GB"] = "Game Boy";
  GamePlatform2["GBC"] = "Game Boy Color";
  GamePlatform2["NES"] = "NES";
  GamePlatform2["SNES"] = "SNES";
  GamePlatform2["GENESIS"] = "Genesis";
  GamePlatform2["N64"] = "N64";
  GamePlatform2["VIRTUAL_BOY"] = "Virtual Boy";
  GamePlatform2["WII_U"] = "Wii U";
  GamePlatform2["XBOX"] = "Xbox";
  GamePlatform2["XBOX_360"] = "Xbox 360";
  GamePlatform2["XBOX_ONE"] = "Xbox One";
  GamePlatform2["XBOX_SERIES"] = "Xbox Series X/S";
  GamePlatform2["WII"] = "Wii";
  GamePlatform2["MAME"] = "MAME";
  GamePlatform2["DOS"] = "DOS";
  GamePlatform2["NEO_GEO"] = "Neo Geo";
  GamePlatform2["SEGA_SATURN"] = "Saturn";
  GamePlatform2["ATARI_2600"] = "Atari 2600";
  GamePlatform2["STEAM_DECK_PC"] = "Steam Deck / PC";
  GamePlatform2["SWITCH2"] = "Switch 2";
  GamePlatform2["ANDROID"] = "Android";
  return GamePlatform2;
})(GamePlatform || {});
var PLATFORM_GROUPS = [
  {
    label: "PC",
    platforms: [
      "DOS" /* DOS */,
      "PC" /* PC */,
      "Steam Deck" /* STEAM_DECK */,
      "Steam Deck / PC" /* STEAM_DECK_PC */
    ]
  },
  {
    label: "Nintendo",
    platforms: [
      "NES" /* NES */,
      "Game Boy" /* GB */,
      "SNES" /* SNES */,
      "Virtual Boy" /* VIRTUAL_BOY */,
      "N64" /* N64 */,
      "Game Boy Color" /* GBC */,
      "Game Boy Advance" /* GBA */,
      "GameCube" /* GAMECUBE */,
      "DS" /* NINTENDO_DS */,
      "Wii" /* WII */,
      "3DS" /* NINTENDO_3DS */,
      "Wii U" /* WII_U */,
      "Switch" /* SWITCH */,
      "Switch 2" /* SWITCH2 */
    ]
  },
  {
    label: "Sony",
    platforms: [
      "Playstation 1" /* PS1 */,
      "Playstation 2" /* PS2 */,
      "Playstation Portable" /* PSP */,
      "Playstation 3" /* PS3 */,
      "Playstation Vita" /* PS_VITA */,
      "Playstation 4" /* PS4 */,
      "Playstation 5" /* PS5 */
    ]
  },
  {
    label: "Microsoft",
    platforms: [
      "Xbox" /* XBOX */,
      "Xbox 360" /* XBOX_360 */,
      "Xbox One" /* XBOX_ONE */,
      "Xbox Series X/S" /* XBOX_SERIES */
    ]
  },
  {
    label: "Sega",
    platforms: [
      "Genesis" /* GENESIS */,
      "Saturn" /* SEGA_SATURN */,
      "Dreamcast" /* DREAMCAST */
    ]
  },
  {
    label: "Arcade / Other",
    platforms: [
      "Atari 2600" /* ATARI_2600 */,
      "Neo Geo" /* NEO_GEO */,
      "MAME" /* MAME */,
      "Android" /* ANDROID */
    ]
  }
];
function generateGameId() {
  return `game-${Date.now()}-${Math.random().toString(36).slice(2, 10)}`;
}
var DEFAULT_SETTINGS = {
  language: "es",
  defaultCoverImage: "",
  defaultPlatform: "PC" /* PC */,
  platformMode: "label",
  scoreType: "stars-5",
  imageDownloadFolder: "",
  cardColor: "#35393d",
  textColor: "",
  noImageBackgroundColor: "",
  noImageTextColor: "",
  steamGridDbApiKey: ""
};
var _GameBacklogPlugin = class _GameBacklogPlugin extends import_obsidian.Plugin {
  t(key, vars) {
    return translate(this.settings.language, key, vars);
  }
  getDateLocale() {
    return this.settings.language === "en" ? "en-US" : "es-ES";
  }
  getScoreBounds() {
    switch (this.settings.scoreType) {
      case "stars-5-half":
        return { min: 0.5, max: 5 };
      case "stars-10":
        return { min: 1, max: 10 };
      case "numeric-10":
        return { min: 0, max: 10 };
      case "stars-5":
      default:
        return { min: 1, max: 5 };
    }
  }
  getScoreStep() {
    return this.settings.scoreType === "stars-5-half" ? 0.5 : 1;
  }
  getDefaultRatingValue() {
    switch (this.settings.scoreType) {
      case "stars-5-half":
        return 3;
      case "stars-10":
      case "numeric-10":
        return 5;
      case "stars-5":
      default:
        return 3;
    }
  }
  normalizeRatingValue(rawValue) {
    const step = this.getScoreStep();
    const value = Number.isFinite(rawValue) ? Math.round(rawValue / step) * step : 0;
    const { max } = this.getScoreBounds();
    return Math.max(0, Math.min(max, value));
  }
  getNumericScoreClass(score) {
    if (score <= 4)
      return "game-score-low";
    if (score <= 7)
      return "game-score-mid";
    return "game-score-high";
  }
  renderStars(container, rating, maxStars) {
    const filledStars = Math.floor(rating);
    const hasHalfStar = rating - filledStars >= 0.5;
    for (let i = 1; i <= maxStars; i++) {
      let starClass = "star empty";
      if (i <= filledStars) {
        starClass = "star filled";
      } else if (i === filledStars + 1 && hasHalfStar) {
        starClass = "star half";
      }
      const star = container.createSpan({ cls: starClass });
      star.textContent = "\u2605";
    }
  }
  renderCardRating(container, rating) {
    const normalizedRating = this.normalizeRatingValue(rating || 0);
    switch (this.settings.scoreType) {
      case "stars-10":
        container.addClass("game-rating-compact");
        container.textContent = normalizedRating > 0 ? `${normalizedRating} \u2B50` : this.t("emptyValue");
        break;
      case "numeric-10": {
        const scoreCircle = container.createSpan({
          cls: `game-score-circle ${this.getNumericScoreClass(normalizedRating)}`,
          text: String(normalizedRating)
        });
        scoreCircle.setAttribute("aria-label", `${this.t("viewRatingLabel")}${normalizedRating}`);
        break;
      }
      case "stars-5":
      case "stars-5-half":
      default:
        this.renderStars(container, normalizedRating, 5);
        break;
    }
  }
  renderDetailRating(container, rating) {
    const normalizedRating = this.normalizeRatingValue(rating || 0);
    switch (this.settings.scoreType) {
      case "stars-10":
        this.renderStars(container, normalizedRating, 10);
        break;
      case "numeric-10": {
        const scoreCircle = container.createSpan({
          cls: `game-score-circle ${this.getNumericScoreClass(normalizedRating)}`,
          text: String(normalizedRating)
        });
        scoreCircle.setAttribute("aria-label", `${this.t("viewRatingLabel")}${normalizedRating}`);
        break;
      }
      case "stars-5":
      case "stars-5-half":
      default:
        this.renderStars(container, normalizedRating, 5);
        break;
    }
  }
  renderTableRating(container, rating) {
    const normalizedRating = this.normalizeRatingValue(rating || 0);
    switch (this.settings.scoreType) {
      case "numeric-10": {
        const scoreCircle = container.createSpan({
          cls: `game-score-circle game-score-circle-small ${this.getNumericScoreClass(normalizedRating)}`,
          text: String(normalizedRating)
        });
        scoreCircle.setAttribute("aria-label", `${this.t("viewRatingLabel")}${normalizedRating}`);
        break;
      }
      case "stars-5-half":
        container.textContent = normalizedRating > 0 ? `${normalizedRating.toFixed(1)} \u2605` : this.t("emptyValue");
        break;
      case "stars-10":
      case "stars-5":
      default:
        container.textContent = normalizedRating > 0 ? `${normalizedRating} \u2605` : this.t("emptyValue");
        break;
    }
  }
  async onload() {
    await this.loadSettings();
    this.addCommand({
      id: "insert-game-backlog-block",
      name: this.t("commandInsertBlock"),
      editorCallback: (editor) => {
        const defaultPlatform = this.settings.defaultPlatform || "PC" /* PC */;
        const defaultBlock = [
          "```game-backlog",
          "---",
          "id: ",
          "name: ",
          "cover: ",
          "rating: 3",
          "date: ",
          `platform: ${defaultPlatform}`,
          "hours: 0",
          "platinum: false",
          "dlc: false",
          "```"
        ].join("\n");
        editor.replaceSelection(defaultBlock);
      }
    });
    this.addCommand({
      id: "insert-game-backlog-profile-block",
      name: this.t("commandInsertProfileBlock"),
      editorCallback: (editor) => {
        const defaultBlock = [
          "```game-backlog-profile",
          "playerName: Player 1",
          "avatar: ",
          "path: ",
          "totalCompleted: ",
          "totalHours: ",
          "platinums: ",
          "mostUsedPlatform: ",
          "files: ",
          "```"
        ].join("\n");
        editor.replaceSelection(defaultBlock);
      }
    });
    this.registerMarkdownCodeBlockProcessor("game-backlog", (source, el, ctx) => {
      try {
        this.renderGameBacklog(source, el, ctx);
      } catch (e) {
        el.createDiv({ text: this.t("renderError") });
      }
    });
    this.registerMarkdownCodeBlockProcessor("game-backlog-profile", async (source, el, ctx) => {
      try {
        await this.renderGameBacklogProfile(source, el, ctx);
      } catch (e) {
        el.createDiv({ text: this.t("renderError") });
      }
    });
    this.addSettingTab(new GameBacklogSettingTab(this.app, this));
  }
  onunload() {
  }
  async loadSettings() {
    var _a;
    const rawData = (_a = await this.loadData()) != null ? _a : {};
    const { showPlatform, ...rest } = rawData;
    const rawPlatformMode = rawData.platformMode === "image" ? "label" : rawData.platformMode;
    const migratedPlatformMode = rawPlatformMode != null ? rawPlatformMode : typeof showPlatform === "boolean" ? showPlatform ? "label" : "none" : DEFAULT_SETTINGS.platformMode;
    this.settings = Object.assign({}, DEFAULT_SETTINGS, rest, { platformMode: migratedPlatformMode });
    if (!rawData.platformMode && typeof showPlatform === "boolean") {
      await this.saveSettings();
    }
  }
  async saveSettings() {
    await this.saveData(this.settings);
  }
  async ensureAdapterFolder(path) {
    const adapter = this.app.vault.adapter;
    if (!path || typeof adapter.mkdir !== "function")
      return;
    const parts = path.split("/").filter(Boolean);
    let currentPath = "";
    for (const part of parts) {
      currentPath = currentPath ? `${currentPath}/${part}` : part;
      if (typeof adapter.exists === "function") {
        const exists = await adapter.exists(currentPath);
        if (exists)
          continue;
      }
      try {
        await adapter.mkdir(currentPath);
      } catch (error) {
        const message = error instanceof Error ? error.message.toLowerCase() : String(error).toLowerCase();
        if (!message.includes("exist")) {
          throw error;
        }
      }
    }
  }
  async downloadPluginAssetsFromRepo() {
    const pluginAssetsFolder = `${this.app.vault.configDir}/plugins/${this.manifest.id}/assets`;
    const adapter = this.app.vault.adapter;
    if (typeof adapter.writeBinary !== "function") {
      new import_obsidian.Notice(this.t("noticeAssetsSyncUnsupported"));
      return;
    }
    new import_obsidian.Notice(this.t("noticeAssetsSyncStarted"));
    try {
      await this.ensureAdapterFolder(pluginAssetsFolder);
      const listingResponse = await (0, import_obsidian.requestUrl)({
        url: _GameBacklogPlugin.REPO_ASSETS_API_URL,
        method: "GET",
        headers: {
          Accept: "application/vnd.github+json",
          "User-Agent": "obsidian-game-backlog-plugin"
        }
      });
      const assets = JSON.parse(listingResponse.text);
      const files = assets.filter((asset) => asset.type === "file" && typeof asset.download_url === "string");
      if (!files.length) {
        new import_obsidian.Notice(this.t("noticeAssetsSyncNoFiles"));
        return;
      }
      let downloadedCount = 0;
      for (const file of files) {
        const fileResponse = await (0, import_obsidian.requestUrl)({
          url: file.download_url,
          method: "GET",
          headers: {
            "User-Agent": "obsidian-game-backlog-plugin"
          }
        });
        await adapter.writeBinary(`${pluginAssetsFolder}/${file.name}`, fileResponse.arrayBuffer);
        downloadedCount += 1;
      }
      new import_obsidian.Notice(this.t("noticeAssetsSyncSuccess", { count: downloadedCount }));
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      new import_obsidian.Notice(this.t("noticeAssetsSyncError", { error: errorMsg }));
    }
  }
  async searchSteamGridDB(term) {
    const response = await (0, import_obsidian.requestUrl)({
      url: `https://www.steamgriddb.com/api/v2/search/autocomplete/${encodeURIComponent(term)}`,
      method: "GET",
      headers: {
        Authorization: `Bearer ${this.settings.steamGridDbApiKey}`,
        "User-Agent": "obsidian-game-backlog-plugin"
      }
    });
    const data = JSON.parse(response.text);
    if (!data.success)
      return [];
    return data.data || [];
  }
  async getSteamGridDBGrids(gameId) {
    const response = await (0, import_obsidian.requestUrl)({
      url: `https://www.steamgriddb.com/api/v2/grids/game/${gameId}`,
      method: "GET",
      headers: {
        Authorization: `Bearer ${this.settings.steamGridDbApiKey}`,
        "User-Agent": "obsidian-game-backlog-plugin"
      }
    });
    const data = JSON.parse(response.text);
    if (!data.success)
      return [];
    return data.data || [];
  }
  parseGameEntries(source) {
    const entries = [];
    const lines = source.trim().split("\n");
    let currentGame = {};
    for (const line of lines) {
      const trimmed = line.trim();
      if (!trimmed || trimmed.startsWith("#"))
        continue;
      if (trimmed.startsWith("---")) {
        if (currentGame.name) {
          if (!currentGame.id) {
            currentGame.id = generateGameId();
          }
          if (!currentGame.platform) {
            currentGame.platform = this.settings.defaultPlatform;
          }
          if (typeof currentGame.platinum !== "boolean") {
            currentGame.platinum = false;
          }
          if (typeof currentGame.dlc !== "boolean") {
            currentGame.dlc = false;
          }
          if (typeof currentGame.hours !== "number") {
            currentGame.hours = 0;
          }
          entries.push(currentGame);
        }
        currentGame = {};
        continue;
      }
      const [key, ...valueParts] = trimmed.split(":");
      const value = valueParts.join(":").trim();
      if (!value)
        continue;
      switch (key.toLowerCase()) {
        case "id":
          currentGame.id = value;
          break;
        case "name":
          currentGame.name = value;
          break;
        case "cover":
          currentGame.cover = value;
          break;
        case "rating":
          currentGame.rating = parseFloat(value) || 0;
          break;
        case "date":
          currentGame.completionDate = value;
          break;
        case "platform":
          currentGame.platform = this.parsePlatform(value);
          break;
        case "hours":
          currentGame.hours = parseFloat(value) || 0;
          break;
        case "platinum":
          currentGame.platinum = ["true", "1", "yes"].includes(value.toLowerCase());
          break;
        case "dlc":
          currentGame.dlc = ["true", "1", "yes"].includes(value.toLowerCase());
          break;
      }
    }
    if (currentGame.name) {
      if (!currentGame.id) {
        currentGame.id = generateGameId();
      }
      if (!currentGame.platform) {
        currentGame.platform = this.settings.defaultPlatform;
      }
      if (typeof currentGame.platinum !== "boolean") {
        currentGame.platinum = false;
      }
      if (typeof currentGame.dlc !== "boolean") {
        currentGame.dlc = false;
      }
      if (typeof currentGame.hours !== "number") {
        currentGame.hours = 0;
      }
      entries.push(currentGame);
    }
    return entries;
  }
  parsePlatform(value) {
    const trimmed = value.trim();
    const platforms = Object.values(GamePlatform);
    const fallbackPlatform = this.settings.defaultPlatform || DEFAULT_SETTINGS.defaultPlatform;
    if (!trimmed) {
      return fallbackPlatform;
    }
    if (platforms.includes(trimmed)) {
      return trimmed;
    }
    const normalized = trimmed.toLowerCase();
    const caseInsensitiveMatch = platforms.find((platform) => platform.toLowerCase() === normalized);
    return caseInsensitiveMatch ? caseInsensitiveMatch : fallbackPlatform;
  }
  async downloadRemoteImage(url, destFolder, sourceFile) {
    var _a;
    try {
      if (!url || !/^https?:\/\//.test(url)) {
        return url;
      }
      const targetFolder = destFolder || ((_a = sourceFile.parent) == null ? void 0 : _a.path) || "";
      const targetPath = this.app.vault.adapter;
      if (typeof targetPath.mkdir === "function" && targetFolder) {
        await targetPath.mkdir(targetFolder);
      }
      const urlParts = new URL(url);
      const fileName = urlParts.pathname.split("/").pop() || `image-${Date.now()}.png`;
      const filePath = targetFolder ? `${targetFolder}/${fileName}` : fileName;
      let arrayBuffer = null;
      let firstAttemptError = null;
      let secondAttemptError = null;
      try {
        const response = await (0, import_obsidian.requestUrl)({
          url,
          method: "GET",
          headers: {
            "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
          }
        });
        arrayBuffer = response.arrayBuffer;
      } catch (error) {
        firstAttemptError = error instanceof Error ? error.message : String(error);
      }
      try {
        if (!arrayBuffer) {
          const fallbackResponse = await (0, import_obsidian.requestUrl)({
            url,
            method: "GET"
          });
          arrayBuffer = fallbackResponse.arrayBuffer;
        }
      } catch (error) {
        secondAttemptError = error instanceof Error ? error.message : String(error);
      }
      if (!arrayBuffer) {
        const attemptDetails = [firstAttemptError, secondAttemptError].filter((detail) => Boolean(detail && detail.trim())).join(" | ");
        throw new Error(
          attemptDetails ? `${this.t("downloadErrorUnavailable")} (${attemptDetails})` : this.t("downloadErrorUnavailable")
        );
      }
      const existingFile = this.app.vault.getAbstractFileByPath(filePath);
      if (existingFile instanceof import_obsidian.TFile) {
        await this.app.vault.modifyBinary(existingFile, arrayBuffer);
      } else {
        await this.app.vault.createBinary(filePath, arrayBuffer);
      }
      return filePath;
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      new import_obsidian.Notice(this.t("downloadErrorPrefix", { error: errorMsg }));
      return url;
    }
  }
  getPluginAssetUrl(fileName) {
    if (!fileName)
      return "";
    const assetPath = `${this.app.vault.configDir}/plugins/${this.manifest.id}/assets/${fileName}`;
    const runtimeAsset = this.app.vault.getAbstractFileByPath(assetPath);
    if (runtimeAsset instanceof import_obsidian.TFile) {
      return this.app.vault.getResourcePath(runtimeAsset);
    }
    const localAssetPath = `assets/${fileName}`;
    const localAsset = this.app.vault.getAbstractFileByPath(localAssetPath);
    if (localAsset instanceof import_obsidian.TFile) {
      return this.app.vault.getResourcePath(localAsset);
    }
    const adapter = this.app.vault.adapter;
    if (typeof adapter.getResourcePath === "function") {
      return adapter.getResourcePath(assetPath);
    }
    return assetPath;
  }
  getPluginAssetFallbackUrl(fileName) {
    if (!fileName)
      return "";
    return `https://raw.githubusercontent.com/KevMorelli/obsidian-game-backlog/main/assets/${encodeURIComponent(fileName)}`;
  }
  resolveImageSource(rawPath, sourcePath = "") {
    if (!rawPath)
      return "";
    const configDirPrefix = `${this.app.vault.configDir}/`;
    if (/^(https?:\/\/|data:|app:|blob:)/i.test(rawPath)) {
      return rawPath;
    }
    if (rawPath.startsWith(configDirPrefix)) {
      const file = this.app.vault.getAbstractFileByPath(rawPath);
      if (file instanceof import_obsidian.TFile) {
        return this.app.vault.getResourcePath(file);
      }
      return "";
    }
    const linked = this.app.metadataCache.getFirstLinkpathDest(rawPath, sourcePath);
    if (linked instanceof import_obsidian.TFile) {
      return this.app.vault.getResourcePath(linked);
    }
    const byPath = this.app.vault.getAbstractFileByPath(rawPath);
    if (byPath instanceof import_obsidian.TFile) {
      return this.app.vault.getResourcePath(byPath);
    }
    return "";
  }
  parseCompletionDate(rawDate) {
    const dateOnlyMatch = rawDate.match(/^(\d{4})-(\d{2})-(\d{2})$/);
    if (dateOnlyMatch) {
      const year = Number(dateOnlyMatch[1]);
      const month = Number(dateOnlyMatch[2]);
      const day = Number(dateOnlyMatch[3]);
      const parsed2 = new Date(year, month - 1, day);
      if (parsed2.getFullYear() === year && parsed2.getMonth() === month - 1 && parsed2.getDate() === day) {
        return parsed2;
      }
      return null;
    }
    const parsed = new Date(rawDate);
    return Number.isNaN(parsed.getTime()) ? null : parsed;
  }
  normalizeCssColor(rawValue) {
    const value = rawValue.trim();
    if (!value)
      return "";
    if (typeof CSS === "undefined" || typeof CSS.supports !== "function") {
      return value;
    }
    return CSS.supports("color", value) ? value : "";
  }
  formatCompletionDate(rawDate) {
    const parsed = this.parseCompletionDate(rawDate);
    if (!parsed)
      return rawDate;
    const locale = this.getDateLocale();
    return new Intl.DateTimeFormat(locale, {
      day: "numeric",
      month: "long"
    }).format(parsed);
  }
  formatCompletionDateWithYear(rawDate) {
    const parsed = this.parseCompletionDate(rawDate);
    if (!parsed)
      return rawDate;
    const locale = this.getDateLocale();
    return new Intl.DateTimeFormat(locale, {
      day: "numeric",
      month: "long",
      year: "numeric"
    }).format(parsed);
  }
  parseBlockConfig(source) {
    const config = { isLocked: false, viewMode: "grid", topGame1: "", topGame2: "", topGame3: "" };
    const lines = source.split("\n");
    for (const line of lines) {
      const trimmed = line.trim();
      if (trimmed.startsWith("---"))
        break;
      const colonIdx = trimmed.indexOf(":");
      if (colonIdx === -1)
        continue;
      const key = trimmed.slice(0, colonIdx).trim().toLowerCase();
      const value = trimmed.slice(colonIdx + 1).trim();
      switch (key) {
        case "islocked":
          config.isLocked = value === "true";
          break;
        case "viewmode":
          config.viewMode = value === "table" ? "table" : "grid";
          break;
        case "topgame1":
          config.topGame1 = value;
          break;
        case "topgame2":
          config.topGame2 = value;
          break;
        case "topgame3":
          config.topGame3 = value;
          break;
      }
    }
    return config;
  }
  async saveBlockConfig(ctx, config) {
    const file = this.app.vault.getAbstractFileByPath(ctx.sourcePath);
    if (!(file instanceof import_obsidian.TFile))
      return;
    const content = await this.app.vault.read(file);
    const codeBlockRegex = /```game-backlog\n([\s\S]*?)```/;
    const match = content.match(codeBlockRegex);
    if (!match)
      return;
    const originalBlock = match[1];
    const firstDashIdx = originalBlock.indexOf("---");
    const entriesPart = firstDashIdx !== -1 ? originalBlock.slice(firstDashIdx) : originalBlock;
    const configSection = `isLocked: ${config.isLocked}
viewMode: ${config.viewMode}
topGame1: ${config.topGame1}
topGame2: ${config.topGame2}
topGame3: ${config.topGame3}
`;
    const newBlock = configSection + entriesPart;
    const newContent = content.replace(codeBlockRegex, "```game-backlog\n" + newBlock + "```");
    await this.app.vault.modify(file, newContent);
  }
  parseProfileConfig(source) {
    const config = {
      playerName: this.t("profileDefaultPlayer"),
      avatar: "",
      path: "",
      totalCompleted: "",
      totalHours: "",
      totalPlatinums: "",
      mostUsedPlatform: "",
      retroAchievementsUrl: "",
      files: []
    };
    let legacyScope = "vault";
    let legacyFolder = "";
    const lines = source.split("\n");
    for (const line of lines) {
      const trimmed = line.trim();
      if (!trimmed || trimmed.startsWith("#"))
        continue;
      const colonIdx = trimmed.indexOf(":");
      if (colonIdx === -1)
        continue;
      const key = trimmed.slice(0, colonIdx).trim().toLowerCase();
      const value = trimmed.slice(colonIdx + 1).trim();
      switch (key) {
        case "player":
        case "playername":
        case "name":
          config.playerName = value || config.playerName;
          break;
        case "avatar":
          config.avatar = value;
          break;
        case "path":
          config.path = value.replace(/^\/+|\/+$/g, "");
          break;
        case "scope":
          legacyScope = value.toLowerCase() === "folder" ? "folder" : "vault";
          break;
        case "folder":
          legacyFolder = value.replace(/^\/+|\/+$/g, "");
          break;
        case "totalcompleted":
          config.totalCompleted = value;
          break;
        case "totalhours":
          config.totalHours = value;
          break;
        case "platinums":
          config.totalPlatinums = value;
          break;
        case "mostusedplatform":
          config.mostUsedPlatform = value;
          break;
        case "retroachievements":
          config.retroAchievementsUrl = value;
          break;
        case "files":
          config.files = value ? value.split("|").map((item) => item.trim()).filter(Boolean) : [];
          break;
        case "file":
          if (value) {
            config.files.push(value);
          }
          break;
      }
    }
    if (!config.path && legacyScope === "folder" && legacyFolder) {
      config.path = legacyFolder;
    }
    config.files = Array.from(new Set(config.files));
    return config;
  }
  extractGameBacklogBlocks(content) {
    const blocks = [];
    const regex = /```game-backlog\s*\n([\s\S]*?)```/g;
    let match;
    while ((match = regex.exec(content)) !== null) {
      blocks.push(match[1]);
    }
    return blocks;
  }
  async collectGamesForProfile(config) {
    const normalizedPath = config.path.trim().replace(/^\/+|\/+$/g, "");
    const markdownFiles = this.app.vault.getMarkdownFiles();
    const candidateFiles = normalizedPath ? markdownFiles.filter((file) => file.path.startsWith(`${normalizedPath}/`)) : markdownFiles;
    const results = await Promise.all(candidateFiles.map(async (file) => {
      const content = await this.app.vault.cachedRead(file);
      const blocks = this.extractGameBacklogBlocks(content);
      if (!blocks.length) {
        return { file, games: [], hasBacklog: false };
      }
      const fileGames = [];
      for (const block of blocks) {
        fileGames.push(...this.parseGameEntries(block));
      }
      return { file, games: fileGames, hasBacklog: true };
    }));
    const games = [];
    const backlogFiles = [];
    for (const result of results) {
      if (result.hasBacklog) {
        backlogFiles.push(result.file);
      }
      games.push(...result.games);
    }
    return { games, backlogFiles };
  }
  getMostUsedPlatform(games) {
    var _a;
    const platformCount = /* @__PURE__ */ new Map();
    for (const game of games) {
      const platform = (game.platform || "").trim();
      if (!platform)
        continue;
      platformCount.set(platform, ((_a = platformCount.get(platform)) != null ? _a : 0) + 1);
    }
    let mostUsed = "";
    let highestCount = 0;
    for (const [platform, count] of platformCount.entries()) {
      if (count > highestCount) {
        mostUsed = platform;
        highestCount = count;
      }
    }
    return mostUsed || this.t("emptyValue");
  }
  isProfileDataEmpty(config) {
    return !config.totalCompleted || !config.totalHours || !config.totalPlatinums || !config.mostUsedPlatform;
  }
  resolveProfileFiles(config) {
    return config.files.map((filePath) => this.app.vault.getAbstractFileByPath(filePath)).filter((file) => file instanceof import_obsidian.TFile);
  }
  async saveProfileBlockData(ctx, config) {
    const file = this.app.vault.getAbstractFileByPath(ctx.sourcePath);
    if (!(file instanceof import_obsidian.TFile))
      return;
    const content = await this.app.vault.read(file);
    const codeBlockRegex = /```game-backlog-profile\n([\s\S]*?)```/;
    if (!codeBlockRegex.test(content))
      return;
    const lines = [
      `playerName: ${config.playerName}`,
      `avatar: ${config.avatar}`,
      `path: ${config.path}`,
      `totalCompleted: ${config.totalCompleted}`,
      `totalHours: ${config.totalHours}`,
      `platinums: ${config.totalPlatinums}`,
      `mostUsedPlatform: ${config.mostUsedPlatform}`,
      `retroAchievements: ${config.retroAchievementsUrl}`
    ];
    if (config.files.length > 0) {
      config.files.forEach((filePath) => {
        lines.push(`file: ${filePath}`);
      });
    } else {
      lines.push("files: ");
    }
    const newContent = content.replace(codeBlockRegex, `\`\`\`game-backlog-profile
${lines.join("\n")}
\`\`\``);
    if (newContent !== content) {
      await this.app.vault.modify(file, newContent);
    }
  }
  async recalculateProfileData(ctx, config) {
    const { games, backlogFiles } = await this.collectGamesForProfile(config);
    const totalCompleted = games.length;
    const totalHours = games.reduce((sum, game) => sum + (game.hours || 0), 0);
    const totalPlatinums = games.filter((game) => game.platinum).length;
    const mostUsedPlatform = this.getMostUsedPlatform(games);
    const formattedHours = Number.isInteger(totalHours) ? String(totalHours) : totalHours.toFixed(1);
    config.totalCompleted = String(totalCompleted);
    config.totalHours = formattedHours;
    config.totalPlatinums = String(totalPlatinums);
    config.mostUsedPlatform = mostUsedPlatform;
    config.files = Array.from(new Set(backlogFiles.map((file) => file.path)));
    await this.saveProfileBlockData(ctx, config);
    return backlogFiles;
  }
  async renderGameBacklogProfile(source, el, ctx) {
    const config = this.parseProfileConfig(source);
    const container = el.createDiv({ cls: "game-backlog-profile-container" });
    const playerName = config.playerName || this.t("profileDefaultPlayer");
    const avatarSource = this.resolveImageSource(config.avatar, ctx.sourcePath);
    let renderProfileContent;
    const appendRefreshToolbar = (parent, disabled = false) => {
      const toolbar = parent.createDiv({ cls: "game-backlog-profile-toolbar" });
      const refreshButton = toolbar.createEl("button", {
        cls: "game-backlog-profile-refresh-button",
        text: "\u{1F504}"
      });
      refreshButton.title = this.t("profileRefreshTooltip");
      refreshButton.setAttribute("aria-label", this.t("profileRefreshTooltip"));
      refreshButton.disabled = disabled;
      refreshButton.addEventListener("click", () => {
        void renderProfileContent(true);
      });
    };
    const renderProfileView = (backlogFiles) => {
      container.empty();
      appendRefreshToolbar(container);
      const header = container.createDiv({ cls: "game-backlog-profile-header" });
      const avatarWrapper = header.createDiv({ cls: "game-backlog-profile-avatar-wrapper" });
      const fallbackAvatar = avatarWrapper.createDiv({ cls: "game-backlog-profile-avatar-fallback" });
      const initials = playerName.split(/\s+/).filter(Boolean).slice(0, 2).map((part) => part.charAt(0).toUpperCase()).join("");
      fallbackAvatar.textContent = initials || "\u{1F3AE}";
      if (avatarSource) {
        const avatar = avatarWrapper.createEl("img", {
          cls: "game-backlog-profile-avatar",
          attr: {
            src: avatarSource,
            alt: playerName
          }
        });
        avatar.onerror = () => {
          avatar.remove();
        };
      }
      const heading = header.createDiv({ cls: "game-backlog-profile-heading" });
      heading.createEl("h3", { cls: "game-backlog-profile-player-name", text: playerName });
      const statsList = container.createDiv({ cls: "game-backlog-profile-stats-list" });
      [
        { emoji: "\u2705", label: this.t("profileStatCompleted"), value: config.totalCompleted },
        { emoji: "\u23F1\uFE0F", label: this.t("profileStatHours"), value: `${config.totalHours} hs` },
        { emoji: "\u{1F3C6}", label: this.t("profileStatPlatinums"), value: config.totalPlatinums },
        { emoji: "\u{1F3AE}", label: this.t("profileStatMostUsedPlatform"), value: config.mostUsedPlatform }
      ].forEach((stat) => {
        const row = statsList.createDiv({ cls: "game-backlog-profile-stat-row" });
        row.createSpan({ cls: "game-backlog-profile-stat-label-inline", text: `${stat.emoji} ${stat.label}` });
        row.createSpan({ cls: "game-backlog-profile-stat-value-inline", text: stat.value || this.t("emptyValue") });
      });
      if (config.retroAchievementsUrl) {
        const raBase = "https://retroachievements.org/user/";
        const isFullUrl = /^https?:\/\//i.test(config.retroAchievementsUrl);
        const raFullUrl = isFullUrl ? config.retroAchievementsUrl : `${raBase}${config.retroAchievementsUrl}`;
        const raUsername = isFullUrl ? config.retroAchievementsUrl.replace(/\/+$/, "").split("/").pop() || config.retroAchievementsUrl : config.retroAchievementsUrl;
        const raRow = statsList.createDiv({ cls: "game-backlog-profile-stat-row game-backlog-profile-ra-row" });
        raRow.createSpan({ cls: "game-backlog-profile-stat-label-inline", text: `\u{1F3C5} ${this.t("profileRetroAchievements")}` });
        const raLink = raRow.createEl("a", {
          cls: "game-backlog-profile-ra-link",
          text: raUsername,
          attr: {
            href: raFullUrl,
            target: "_blank",
            rel: "noopener noreferrer"
          }
        });
        raLink.addEventListener("click", (event) => {
          event.preventDefault();
          window.open(raFullUrl, "_blank", "noopener,noreferrer");
        });
      }
      if (backlogFiles.length > 0) {
        const filesSection = container.createDiv({ cls: "game-backlog-profile-files" });
        const filesList = filesSection.createDiv({ cls: "game-backlog-profile-files-list" });
        [...backlogFiles].sort((a, b) => a.basename.localeCompare(b.basename, void 0, { sensitivity: "base" })).forEach((file) => {
          const link = filesList.createEl("a", {
            cls: "game-backlog-profile-file-link",
            text: file.basename,
            attr: {
              href: "#",
              title: file.path
            }
          });
          link.addEventListener("click", (event) => {
            event.preventDefault();
            void this.app.workspace.getLeaf(false).openFile(file);
          });
        });
      }
      if (config.totalCompleted === "0") {
        container.createDiv({ cls: "game-backlog-profile-empty", text: this.t("profileNoGames") });
      }
    };
    renderProfileContent = async (forceRefresh = false) => {
      const shouldRefresh = forceRefresh || this.isProfileDataEmpty(config);
      if (shouldRefresh) {
        container.empty();
        appendRefreshToolbar(container, true);
        container.createDiv({ cls: "game-backlog-profile-loading", text: this.t("profileLoading") });
        try {
          const backlogFiles = await this.recalculateProfileData(ctx, config);
          renderProfileView(backlogFiles);
        } catch (e) {
          container.empty();
          appendRefreshToolbar(container);
          container.createDiv({ cls: "game-backlog-profile-empty", text: this.t("renderError") });
        }
        return;
      }
      renderProfileView(this.resolveProfileFiles(config));
    };
    await renderProfileContent();
  }
  renderGameBacklog(source, el, ctx) {
    const games = this.parseGameEntries(source);
    const blockConfig = this.parseBlockConfig(source);
    const container = el.createDiv({ cls: "game-backlog-container" });
    let isTableView = blockConfig.viewMode === "table";
    const controlsContainer = container.createDiv({ cls: "game-backlog-controls" });
    const toggleButton = controlsContainer.createEl("button", { cls: "game-view-toggle", text: isTableView ? "\u{1F0CF}" : "\u{1F4CA}" });
    toggleButton.title = this.t("toggleViewTitle");
    const lockButton = controlsContainer.createEl("button", { cls: "game-lock-toggle" });
    lockButton.textContent = blockConfig.isLocked ? "\u{1F512}" : "\u{1F513}";
    lockButton.title = blockConfig.isLocked ? this.t("lockTitleLocked") : this.t("lockTitleUnlocked");
    lockButton.addEventListener("click", () => {
      void (async () => {
        blockConfig.isLocked = !blockConfig.isLocked;
        await this.saveBlockConfig(ctx, blockConfig);
        lockButton.textContent = blockConfig.isLocked ? "\u{1F512}" : "\u{1F513}";
        lockButton.title = blockConfig.isLocked ? this.t("lockTitleLocked") : this.t("lockTitleUnlocked");
        if (isTableView) {
          renderTable();
        } else {
          renderCards();
        }
        statsContainer.empty();
        renderStats();
      })();
    });
    const cardsContainer = container.createDiv({ cls: "game-backlog-cards-view" });
    const tableContainer = container.createDiv({ cls: "game-backlog-table-view" });
    const setContainerVisibility = (target, visible) => {
      target.classList.toggle("game-backlog-hidden", !visible);
    };
    const statsContainer = container.createDiv({ cls: "game-stats-container" });
    const renderCards = () => {
      cardsContainer.empty();
      const grid = cardsContainer.createDiv({ cls: "game-backlog-grid" });
      games.forEach((game) => {
        let card;
        if (game.platinum) {
          const wrapper = grid.createDiv({ cls: "game-card-platinum-wrapper" });
          card = wrapper.createDiv({ cls: "game-card" });
        } else {
          card = grid.createDiv({ cls: "game-card" });
        }
        const cardColor = this.normalizeCssColor(this.settings.cardColor || "");
        const textColor = this.normalizeCssColor(this.settings.textColor || "");
        if (cardColor) {
          card.style.setProperty("--game-card-bg", cardColor);
        }
        if (textColor) {
          card.style.setProperty("--game-card-text", textColor);
        }
        const coverContainer = card.createDiv({ cls: "game-cover-container" });
        const noImageBackgroundColor = this.normalizeCssColor(this.settings.noImageBackgroundColor || "");
        const noImageTextColor = this.normalizeCssColor(this.settings.noImageTextColor || "");
        if (noImageBackgroundColor) {
          coverContainer.style.setProperty("--game-no-image-bg", noImageBackgroundColor);
        }
        if (noImageTextColor) {
          coverContainer.style.setProperty("--game-no-image-text", noImageTextColor);
        }
        const nameOverlay = coverContainer.createDiv({ cls: "game-name-overlay" });
        nameOverlay.textContent = game.name || this.t("noName");
        const normalizeCoverValue = (value) => {
          const normalized = (value || "").trim();
          const emptyAliases = ["null", "undefined", "none", "n/a", "na", "-"];
          return emptyAliases.includes(normalized.toLowerCase()) ? "" : normalized;
        };
        const gameCover = normalizeCoverValue(game.cover || "");
        const defaultCover = normalizeCoverValue(this.settings.defaultCoverImage || "");
        const resolvedGameCover = this.resolveImageSource(gameCover, ctx.sourcePath);
        const resolvedDefaultCover = this.resolveImageSource(defaultCover, ctx.sourcePath);
        const coverSource = resolvedGameCover || resolvedDefaultCover;
        if (coverSource) {
          const cover = coverContainer.createEl("img", {
            cls: "game-cover",
            attr: {
              src: coverSource,
              alt: game.name || this.t("noName")
            }
          });
          cover.onerror = () => {
            if (resolvedDefaultCover && coverSource !== resolvedDefaultCover) {
              cover.src = resolvedDefaultCover;
              return;
            }
            cover.remove();
            coverContainer.addClass("game-cover-no-image");
            nameOverlay.addClass("game-name-overlay-static");
          };
        } else {
          coverContainer.addClass("game-cover-no-image");
          nameOverlay.addClass("game-name-overlay-static");
        }
        if (game.platinum) {
          const platinumLocalSource = this.getPluginAssetUrl("Platinum.png");
          const platinumFallbackSource = this.getPluginAssetFallbackUrl("Platinum.png");
          const initialPlatinumSource = platinumLocalSource || platinumFallbackSource;
          let triedPlatinumFallback = !platinumLocalSource;
          const platinumBadge = coverContainer.createEl("img", {
            cls: "game-platinum-badge",
            attr: {
              src: initialPlatinumSource,
              alt: this.t("platinumBadgeAlt")
            }
          });
          platinumBadge.onerror = () => {
            if (!triedPlatinumFallback && platinumFallbackSource) {
              triedPlatinumFallback = true;
              platinumBadge.src = platinumFallbackSource;
              return;
            }
            platinumBadge.remove();
          };
        }
        if (game.dlc) {
          const dlcBadge = coverContainer.createDiv({ cls: "game-dlc-badge" });
          dlcBadge.textContent = this.t("dlcBadgeAlt");
        }
        const info = card.createDiv({ cls: "game-info" });
        const ratingContainer = info.createDiv({ cls: "game-rating" });
        this.renderCardRating(ratingContainer, game.rating || 0);
        const details = info.createDiv({ cls: "game-details" });
        if (game.completionDate) {
          const date = details.createDiv({ cls: "game-date" });
          date.textContent = `\u{1F4C5} ${this.formatCompletionDate(game.completionDate)}`;
        }
        if (this.settings.platformMode === "label" && game.platform) {
          const platform = details.createDiv({ cls: "game-platform" });
          platform.textContent = `\u{1F3AE} ${game.platform}`;
        }
        if (game.hours && game.hours > 0) {
          const hours = details.createDiv({ cls: "game-hours" });
          hours.textContent = `\u23F1\uFE0F ${game.hours} hs`;
        }
        card.addEventListener("click", (e) => {
          e.stopPropagation();
          new GameViewModal(this.app, game, (editedGame) => {
            void this.editGameInFile(ctx, game, editedGame);
          }, () => {
            void this.deleteGameInFile(ctx, game);
          }, !blockConfig.isLocked, this).open();
        });
      });
      if (!blockConfig.isLocked) {
        const addCard = grid.createDiv({ cls: "game-card game-card-add" });
        addCard.createSpan({ cls: "game-card-add-icon", text: "+" });
        addCard.addEventListener("click", () => {
          new AddGameModal(this.app, (newGame) => {
            void this.addGameToFile(ctx, newGame);
          }, this).open();
        });
      }
    };
    const renderTable = () => {
      tableContainer.empty();
      const table = tableContainer.createEl("table", { cls: "game-backlog-table" });
      const thead = table.createEl("thead");
      const headerRow = thead.createEl("tr");
      [this.t("tableHeaderGame"), this.t("tableHeaderPlatform"), this.t("tableHeaderDate"), this.t("tableHeaderRating"), this.t("tableHeaderDuration")].forEach((header) => {
        headerRow.createEl("th", { text: header });
      });
      const tbody = table.createEl("tbody");
      games.forEach((game) => {
        const row = tbody.createEl("tr");
        row.addClass("game-clickable-row");
        const nameCell = row.createEl("td");
        nameCell.addClass("game-table-name-cell");
        const gameName = game.name || this.t("noName");
        const nameContent = nameCell.createSpan({ cls: "game-table-name-content" });
        const platinumIcon = nameContent.createSpan({ cls: "game-table-platinum-icon" });
        if (game.platinum) {
          platinumIcon.textContent = "\u{1F3C6}";
        } else {
          platinumIcon.addClass("is-empty");
          platinumIcon.textContent = "\u{1F3C6}";
        }
        const dlcIcon = nameContent.createSpan({ cls: "game-table-dlc-icon" });
        if (game.dlc) {
          dlcIcon.textContent = "\u{1F9E9}";
        } else {
          dlcIcon.addClass("is-empty");
          dlcIcon.textContent = "\u{1F9E9}";
        }
        const nameText = nameContent.createSpan({ cls: "game-table-name-text" });
        nameText.textContent = gameName;
        const platformCell = row.createEl("td");
        platformCell.textContent = game.platform || this.t("emptyValue");
        const dateCell = row.createEl("td");
        if (game.completionDate) {
          const parsed = this.parseCompletionDate(game.completionDate);
          if (parsed) {
            const day = String(parsed.getDate()).padStart(2, "0");
            const month = String(parsed.getMonth() + 1).padStart(2, "0");
            dateCell.textContent = `${day}/${month}`;
          } else {
            dateCell.textContent = game.completionDate;
          }
        } else {
          dateCell.textContent = this.t("emptyValue");
        }
        const scoreCell = row.createEl("td");
        this.renderTableRating(scoreCell, game.rating || 0);
        const hoursCell = row.createEl("td");
        if (game.hours && game.hours > 0) {
          hoursCell.textContent = `${game.hours} hs`;
        } else {
          hoursCell.textContent = this.t("emptyValue");
        }
        row.addEventListener("click", () => {
          new GameViewModal(this.app, game, (editedGame) => {
            void this.editGameInFile(ctx, game, editedGame);
          }, () => {
            void this.deleteGameInFile(ctx, game);
          }, !blockConfig.isLocked, this).open();
        });
      });
      if (!blockConfig.isLocked) {
        const addRow = tbody.createEl("tr", { cls: "game-table-add-row" });
        const addCell = addRow.createEl("td", { attr: { colspan: "5" } });
        addCell.textContent = "+";
        addRow.addEventListener("click", () => {
          new AddGameModal(this.app, (newGame) => {
            void this.addGameToFile(ctx, newGame);
          }, this).open();
        });
      }
    };
    const renderStats = () => {
      const totalGames = games.filter((g) => !g.dlc).length;
      const totalDlcs = games.filter((g) => g.dlc).length;
      const totalHours = games.reduce((sum, game) => sum + (game.hours || 0), 0);
      const statsLabels = statsContainer.createDiv({ cls: "game-stats-labels" });
      const gamesLabel = statsLabels.createDiv({ cls: "game-stat-item" });
      gamesLabel.textContent = this.t("statsCompleted", { count: totalGames });
      if (totalDlcs > 0) {
        const dlcsLabel = statsLabels.createDiv({ cls: "game-stat-item" });
        dlcsLabel.textContent = this.t("statsDlcCompleted", { count: totalDlcs });
      }
      const hoursLabel = statsLabels.createDiv({ cls: "game-stat-item" });
      hoursLabel.textContent = this.t("statsTotalHours", { hours: Math.round(totalHours) });
      const topGamesContainer = statsContainer.createDiv({ cls: "game-top-games" });
      const medals = ["\u{1F947}", "\u{1F948}", "\u{1F949}"];
      const topGameSettings = ["topGame1", "topGame2", "topGame3"];
      const gameNames = games.map((g) => g.name);
      topGameSettings.forEach((settingKey, index) => {
        const topGameRow = topGamesContainer.createDiv({ cls: "game-top-game-row" });
        const medal = topGameRow.createSpan({ cls: "game-medal" });
        medal.textContent = medals[index];
        const currentValue = blockConfig[settingKey];
        if (blockConfig.isLocked) {
          const label = topGameRow.createSpan({ cls: "game-top-game-label" });
          label.textContent = currentValue || this.t("emptyValue");
        } else {
          const select = topGameRow.createEl("select", { cls: "game-top-game-select" });
          const emptyOption = select.createEl("option");
          emptyOption.value = "";
          emptyOption.textContent = [this.t("topGameSelectBest"), this.t("topGameSelectSecond"), this.t("topGameSelectThird")][index];
          gameNames.forEach((name) => {
            const option = select.createEl("option");
            option.value = name;
            option.textContent = name;
          });
          select.value = currentValue || "";
          select.addEventListener("change", (e) => {
            void (async () => {
              const newValue = e.target.value;
              blockConfig[settingKey] = newValue;
              await this.saveBlockConfig(ctx, blockConfig);
            })();
          });
        }
      });
    };
    if (isTableView) {
      setContainerVisibility(cardsContainer, false);
      setContainerVisibility(tableContainer, true);
      renderTable();
    } else {
      setContainerVisibility(cardsContainer, true);
      setContainerVisibility(tableContainer, false);
      renderCards();
    }
    statsContainer.empty();
    renderStats();
    toggleButton.addEventListener("click", () => {
      void (async () => {
        isTableView = !isTableView;
        blockConfig.viewMode = isTableView ? "table" : "grid";
        await this.saveBlockConfig(ctx, blockConfig);
        if (isTableView) {
          setContainerVisibility(cardsContainer, false);
          setContainerVisibility(tableContainer, true);
          toggleButton.textContent = "\u{1F0CF}";
          renderTable();
        } else {
          setContainerVisibility(cardsContainer, true);
          setContainerVisibility(tableContainer, false);
          toggleButton.textContent = "\u{1F4CA}";
        }
      })();
    });
  }
  async addGameToFile(ctx, game) {
    const file = this.app.vault.getAbstractFileByPath(ctx.sourcePath);
    if (!(file instanceof import_obsidian.TFile))
      return;
    const content = await this.app.vault.read(file);
    const codeBlockRegex = /```game-backlog\n([\s\S]*?)```/;
    const match = content.match(codeBlockRegex);
    if (match) {
      const gameId = game.id || generateGameId();
      const newEntry = `
---
id: ${gameId}
name: ${game.name}
cover: ${game.cover}
rating: ${game.rating}
date: ${game.completionDate}
platform: ${game.platform}
hours: ${game.hours}
platinum: ${game.platinum}
dlc: ${game.dlc}
`;
      const updatedBlock = match[1] + newEntry;
      const newContent = content.replace(codeBlockRegex, `\`\`\`game-backlog
${updatedBlock}\`\`\``);
      await this.app.vault.modify(file, newContent);
      new import_obsidian.Notice(this.t("noticeGameAdded"));
    }
  }
  async editGameInFile(ctx, oldGame, newGame) {
    const file = this.app.vault.getAbstractFileByPath(ctx.sourcePath);
    if (!(file instanceof import_obsidian.TFile))
      return;
    const content = await this.app.vault.read(file);
    const codeBlockRegex = /```game-backlog\n([\s\S]*?)```/;
    const match = content.match(codeBlockRegex);
    if (match) {
      const oldId = oldGame.id || "";
      const stableId = newGame.id || oldId || generateGameId();
      const newEntry = `---
id: ${stableId}
name: ${newGame.name}
cover: ${newGame.cover}
rating: ${newGame.rating}
date: ${newGame.completionDate}
platform: ${newGame.platform}
hours: ${newGame.hours}
platinum: ${newGame.platinum}
dlc: ${newGame.dlc}
`;
      const originalBlock = match[1];
      const entries = originalBlock.match(/---\s*\n[\s\S]*?(?=(?:\n---\s*\n)|$)/g) || [];
      let entryIndex = -1;
      if (oldId) {
        entryIndex = entries.findIndex((entry) => {
          const idMatch = entry.match(/^id:\s*(.+)$/im);
          return ((idMatch == null ? void 0 : idMatch[1]) || "").trim() === oldId;
        });
      }
      if (entryIndex === -1) {
        entryIndex = entries.findIndex((entry) => {
          var _a, _b;
          const nameMatch = entry.match(/^name:\s*(.*)$/im);
          const coverMatch = entry.match(/^cover:\s*(.*)$/im);
          const ratingMatch = entry.match(/^rating:\s*(.*)$/im);
          const dateMatch = entry.match(/^date:\s*(.*)$/im);
          const platformMatch = entry.match(/^platform:\s*(.*)$/im);
          const hoursMatch = entry.match(/^hours:\s*(.*)$/im);
          const platinumMatch = entry.match(/^platinum:\s*(.*)$/im);
          return ((nameMatch == null ? void 0 : nameMatch[1]) || "").trim() === (oldGame.name || "").trim() && ((coverMatch == null ? void 0 : coverMatch[1]) || "").trim() === (oldGame.cover || "").trim() && ((ratingMatch == null ? void 0 : ratingMatch[1]) || "").trim() === String((_a = oldGame.rating) != null ? _a : 0) && ((dateMatch == null ? void 0 : dateMatch[1]) || "").trim() === (oldGame.completionDate || "").trim() && ((platformMatch == null ? void 0 : platformMatch[1]) || "").trim() === String(oldGame.platform || "").trim() && ((hoursMatch == null ? void 0 : hoursMatch[1]) || "").trim() === String((_b = oldGame.hours) != null ? _b : 0) && ((platinumMatch == null ? void 0 : platinumMatch[1]) || "").trim().toLowerCase() === String(oldGame.platinum).toLowerCase();
        });
      }
      if (entryIndex === -1) {
        new import_obsidian.Notice(this.t("noticeEntryNotFound"));
        return;
      }
      const targetEntry = entries[entryIndex];
      const updatedBlock = originalBlock.replace(targetEntry, newEntry);
      const newContent = content.replace(codeBlockRegex, `\`\`\`game-backlog
${updatedBlock}\`\`\``);
      await this.app.vault.modify(file, newContent);
      new import_obsidian.Notice(this.t("noticeGameUpdated"));
    }
  }
  async deleteGameInFile(ctx, gameToDelete) {
    const file = this.app.vault.getAbstractFileByPath(ctx.sourcePath);
    if (!(file instanceof import_obsidian.TFile))
      return;
    const content = await this.app.vault.read(file);
    const codeBlockRegex = /```game-backlog\n([\s\S]*?)```/;
    const match = content.match(codeBlockRegex);
    if (match) {
      const targetId = gameToDelete.id || "";
      const originalBlock = match[1];
      const entries = originalBlock.match(/---\s*\n[\s\S]*?(?=(?:\n---\s*\n)|$)/g) || [];
      let entryIndex = -1;
      if (targetId) {
        entryIndex = entries.findIndex((entry) => {
          const idMatch = entry.match(/^id:\s*(.+)$/im);
          return ((idMatch == null ? void 0 : idMatch[1]) || "").trim() === targetId;
        });
      }
      if (entryIndex === -1) {
        entryIndex = entries.findIndex((entry) => {
          var _a, _b;
          const nameMatch = entry.match(/^name:\s*(.*)$/im);
          const coverMatch = entry.match(/^cover:\s*(.*)$/im);
          const ratingMatch = entry.match(/^rating:\s*(.*)$/im);
          const dateMatch = entry.match(/^date:\s*(.*)$/im);
          const platformMatch = entry.match(/^platform:\s*(.*)$/im);
          const hoursMatch = entry.match(/^hours:\s*(.*)$/im);
          const platinumMatch = entry.match(/^platinum:\s*(.*)$/im);
          return ((nameMatch == null ? void 0 : nameMatch[1]) || "").trim() === (gameToDelete.name || "").trim() && ((coverMatch == null ? void 0 : coverMatch[1]) || "").trim() === (gameToDelete.cover || "").trim() && ((ratingMatch == null ? void 0 : ratingMatch[1]) || "").trim() === String((_a = gameToDelete.rating) != null ? _a : 0) && ((dateMatch == null ? void 0 : dateMatch[1]) || "").trim() === (gameToDelete.completionDate || "").trim() && ((platformMatch == null ? void 0 : platformMatch[1]) || "").trim() === String(gameToDelete.platform || "").trim() && ((hoursMatch == null ? void 0 : hoursMatch[1]) || "").trim() === String((_b = gameToDelete.hours) != null ? _b : 0) && ((platinumMatch == null ? void 0 : platinumMatch[1]) || "").trim().toLowerCase() === String(gameToDelete.platinum).toLowerCase();
        });
      }
      if (entryIndex === -1) {
        new import_obsidian.Notice(this.t("noticeEntryNotFound"));
        return;
      }
      const targetEntry = entries[entryIndex];
      const updatedBlock = originalBlock.replace(targetEntry, "").replace(/\n{3,}/g, "\n\n");
      const newContent = content.replace(codeBlockRegex, `\`\`\`game-backlog
${updatedBlock}\`\`\``);
      await this.app.vault.modify(file, newContent);
      new import_obsidian.Notice(this.t("noticeGameDeleted"));
    }
  }
};
_GameBacklogPlugin.REPO_ASSETS_API_URL = "https://api.github.com/repos/KevMorelli/obsidian-game-backlog/contents/assets";
var GameBacklogPlugin = _GameBacklogPlugin;
var AddGameModal = class extends import_obsidian.Modal {
  constructor(app, onSubmit, plugin, existingGame) {
    super(app);
    this.id = "";
    this.name = "";
    this.cover = "";
    this.rating = 3;
    this.completionDate = "";
    this.platform = "PC" /* PC */;
    this.hours = 0;
    this.platinum = false;
    this.dlc = false;
    this.onSubmit = onSubmit;
    this.plugin = plugin;
    this.isEditMode = !!existingGame;
    if (existingGame) {
      this.id = existingGame.id || "";
      this.name = existingGame.name || "";
      this.cover = existingGame.cover || "";
      this.rating = typeof existingGame.rating === "number" ? existingGame.rating : this.plugin.getDefaultRatingValue();
      this.completionDate = existingGame.completionDate || "";
      this.platform = existingGame.platform || this.plugin.settings.defaultPlatform;
      this.hours = existingGame.hours || 0;
      this.platinum = existingGame.platinum || false;
      this.dlc = existingGame.dlc || false;
    } else {
      this.rating = this.plugin.getDefaultRatingValue();
      this.platform = this.plugin.settings.defaultPlatform;
      const today = /* @__PURE__ */ new Date();
      this.completionDate = today.toISOString().split("T")[0];
    }
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.createEl("h2", { text: this.isEditMode ? this.plugin.t("modalEditTitle") : this.plugin.t("modalAddTitle") });
    new import_obsidian.Setting(contentEl).setName(this.plugin.t("modalNameLabel")).setDesc(this.plugin.t("modalNameDesc")).addText((text) => text.setPlaceholder(this.plugin.t("modalNamePlaceholder")).setValue(this.name).onChange((value) => {
      this.name = value;
    }));
    let coverTextField;
    let downloadBtnEl = null;
    const updateDownloadButtonVisibility = () => {
      if (downloadBtnEl) {
        downloadBtnEl.style.display = /^https?:\/\//.test(this.cover) ? "" : "none";
      }
    };
    const coverSetting = new import_obsidian.Setting(contentEl);
    coverSetting.setName(this.plugin.t("modalCoverLabel")).setDesc(this.plugin.t("modalCoverDesc")).addText((text) => {
      coverTextField = text.inputEl;
      text.setPlaceholder(this.plugin.t("modalCoverPlaceholder")).setValue(this.cover).onChange((value) => {
        this.cover = value;
        updateDownloadButtonVisibility();
      });
    }).addButton((btn) => {
      downloadBtnEl = btn.buttonEl;
      btn.setButtonText("\u2B07\uFE0F").setTooltip(this.plugin.t("modalDownloadTooltip")).onClick(async () => {
        const activeFile = this.app.workspace.getActiveFile();
        if (!activeFile) {
          new import_obsidian.Notice(this.plugin.t("noticeNoActiveFile"));
          return;
        }
        const downloadPath = await this.plugin.downloadRemoteImage(
          this.cover,
          this.plugin.settings.imageDownloadFolder || null,
          activeFile
        );
        if (downloadPath !== this.cover) {
          this.cover = downloadPath;
          if (coverTextField) {
            coverTextField.value = downloadPath;
            coverTextField.dispatchEvent(new Event("input", { bubbles: true }));
          }
          new import_obsidian.Notice(this.plugin.t("noticeImageDownloaded"));
        }
      });
      btn.buttonEl.style.display = /^https?:\/\//.test(this.cover) ? "" : "none";
    });
    if (this.plugin.settings.steamGridDbApiKey) {
      coverSetting.addButton((btn) => btn.setButtonText("\u{1F50D}").setTooltip(this.plugin.t("steamGridDbSearchTooltip")).onClick(() => {
        new SteamGridDBSearchModal(this.app, this.plugin, this.name, (url) => {
          this.cover = url;
          if (coverTextField) {
            coverTextField.value = url;
            coverTextField.dispatchEvent(new Event("input", { bubbles: true }));
          }
          updateDownloadButtonVisibility();
        }).open();
      }));
    }
    const ratingBounds = this.plugin.getScoreBounds();
    const ratingSetting = new import_obsidian.Setting(contentEl).setName(this.plugin.t("modalRatingLabel")).setDesc(this.plugin.t("modalRatingDesc"));
    if (this.plugin.settings.scoreType === "numeric-10") {
      ratingSetting.addText((text) => text.setPlaceholder("0").setValue(String(this.plugin.normalizeRatingValue(this.rating))).onChange((value) => {
        const parsed = parseInt(value, 10);
        this.rating = Number.isFinite(parsed) ? Math.max(ratingBounds.min, Math.min(ratingBounds.max, parsed)) : ratingBounds.min;
      }).inputEl.type = "number");
      const inputEl = ratingSetting.controlEl.querySelector("input");
      if (inputEl instanceof HTMLInputElement) {
        inputEl.min = String(ratingBounds.min);
        inputEl.max = String(ratingBounds.max);
        inputEl.step = "1";
      }
    } else if (this.plugin.settings.scoreType === "stars-5" || this.plugin.settings.scoreType === "stars-5-half") {
      const starCount = 5;
      const allowHalfStars = this.plugin.settings.scoreType === "stars-5-half";
      let currentRating = Math.max(ratingBounds.min, Math.min(starCount, this.plugin.normalizeRatingValue(this.rating)));
      let hoverRating = null;
      const starContainer = ratingSetting.controlEl.createDiv({ cls: "game-modal-star-picker" });
      const getStarClass = (starIndex, value) => {
        if (value >= starIndex + 1)
          return "star filled";
        if (value >= starIndex + 0.5)
          return "star half";
        return "star empty";
      };
      const renderStars = () => {
        starContainer.empty();
        const activeRating = hoverRating != null ? hoverRating : currentRating;
        for (let i = 1; i <= starCount; i++) {
          const star = starContainer.createSpan({
            cls: `${getStarClass(i - 1, activeRating)} game-modal-star`,
            text: "\u2605"
          });
          if (allowHalfStars) {
            const getHalfValue = (event) => {
              const rect = star.getBoundingClientRect();
              const relativeX = event.clientX - rect.left;
              const half = relativeX <= rect.width / 2 ? 0.5 : 1;
              return i - 1 + half;
            };
            star.addEventListener("mousemove", (event) => {
              hoverRating = getHalfValue(event);
              renderStars();
            });
            star.addEventListener("click", (event) => {
              currentRating = getHalfValue(event);
              this.rating = currentRating;
              renderStars();
            });
          } else {
            star.addEventListener("mouseenter", () => {
              hoverRating = i;
              renderStars();
            });
            star.addEventListener("click", () => {
              currentRating = i;
              this.rating = i;
              renderStars();
            });
          }
        }
      };
      starContainer.addEventListener("mouseleave", () => {
        hoverRating = null;
        renderStars();
      });
      renderStars();
    } else {
      ratingSetting.addSlider((slider) => slider.setLimits(ratingBounds.min, ratingBounds.max, this.plugin.settings.scoreType === "stars-5-half" ? 0.5 : 1).setValue(Math.max(ratingBounds.min, Math.min(ratingBounds.max, this.rating))).setDynamicTooltip().onChange((value) => {
        this.rating = value;
      }));
    }
    new import_obsidian.Setting(contentEl).setName(this.plugin.t("modalCompletionDateLabel")).setDesc(this.plugin.t("modalCompletionDateDesc")).addText((text) => text.setPlaceholder("Completion date").setValue(this.completionDate).onChange((value) => {
      this.completionDate = value;
    }).inputEl.type = "date");
    new import_obsidian.Setting(contentEl).setName(this.plugin.t("modalPlatformLabel")).setDesc(this.plugin.t("modalPlatformDesc")).addDropdown((dropdown) => {
      const selectEl = dropdown.selectEl;
      PLATFORM_GROUPS.forEach((group, groupIndex) => {
        const groupHeader = document.createElement("option");
        groupHeader.textContent = `--- ${group.label} ---`;
        groupHeader.disabled = true;
        groupHeader.value = "";
        selectEl.appendChild(groupHeader);
        group.platforms.forEach((platform) => {
          dropdown.addOption(platform, platform);
        });
      });
      dropdown.setValue(this.platform).onChange((value) => {
        this.platform = value;
      });
    });
    new import_obsidian.Setting(contentEl).setName(this.plugin.t("modalHoursLabel")).setDesc(this.plugin.t("modalHoursDesc")).addText((text) => text.setPlaceholder("0").setValue(String(this.hours)).onChange((value) => {
      this.hours = parseFloat(value) || 0;
    }).inputEl.type = "number");
    new import_obsidian.Setting(contentEl).setName(this.plugin.t("modalPlatinumLabel")).setDesc(this.plugin.t("modalPlatinumDesc")).addToggle((toggle) => toggle.setValue(this.platinum).onChange((value) => {
      this.platinum = value;
    }));
    new import_obsidian.Setting(contentEl).setName(this.plugin.t("modalDlcLabel")).setDesc(this.plugin.t("modalDlcDesc")).addToggle((toggle) => toggle.setValue(this.dlc).onChange((value) => {
      this.dlc = value;
    }));
    new import_obsidian.Setting(contentEl).addButton((btn) => btn.setButtonText(this.isEditMode ? this.plugin.t("buttonSave") : this.plugin.t("buttonAdd")).setCta().onClick(() => {
      this.onSubmit({
        id: this.id || generateGameId(),
        name: this.name,
        cover: this.cover,
        rating: this.rating,
        completionDate: this.completionDate,
        platform: this.platform,
        hours: this.hours,
        platinum: this.platinum,
        dlc: this.dlc
      });
      this.close();
    })).addButton((btn) => btn.setButtonText(this.plugin.t("buttonCancel")).onClick(() => {
      this.close();
    }));
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};
var GameViewModal = class extends import_obsidian.Modal {
  constructor(app, game, onEdit, onDelete, canEdit = true, plugin) {
    super(app);
    this.game = game;
    this.onEdit = onEdit;
    this.onDelete = onDelete;
    this.canEdit = canEdit;
    this.plugin = plugin;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.createEl("h2", { text: this.game.name || this.plugin.t("noName") });
    const infoContainer = contentEl.createDiv({ cls: "game-view-info" });
    const ratingContainer = infoContainer.createDiv({ cls: "game-view-item" });
    const ratingLabel = ratingContainer.createSpan({ cls: "game-view-label" });
    ratingLabel.textContent = this.plugin.t("viewRatingLabel");
    const ratingValue = ratingContainer.createSpan({ cls: "game-view-value" });
    this.plugin.renderDetailRating(ratingValue, this.game.rating || 0);
    if (this.game.platform) {
      const platformContainer = infoContainer.createDiv({ cls: "game-view-item" });
      const platformLabel = platformContainer.createSpan({ cls: "game-view-label" });
      platformLabel.textContent = this.plugin.t("viewPlatformLabel");
      const platformValue = platformContainer.createSpan({ cls: "game-view-value" });
      platformValue.textContent = this.game.platform;
    }
    if (this.game.completionDate) {
      const dateContainer = infoContainer.createDiv({ cls: "game-view-item" });
      const dateLabel = dateContainer.createSpan({ cls: "game-view-label" });
      dateLabel.textContent = this.plugin.t("viewCompletedLabel");
      const dateValue = dateContainer.createSpan({ cls: "game-view-value" });
      dateValue.textContent = this.plugin.formatCompletionDateWithYear(this.game.completionDate);
    }
    if (this.game.hours && this.game.hours > 0) {
      const hoursContainer = infoContainer.createDiv({ cls: "game-view-item" });
      const hoursLabel = hoursContainer.createSpan({ cls: "game-view-label" });
      hoursLabel.textContent = this.plugin.t("viewHoursLabel");
      const hoursValue = hoursContainer.createSpan({ cls: "game-view-value" });
      hoursValue.textContent = `${this.game.hours} hs`;
    }
    if (this.game.platinum) {
      const platinumContainer = infoContainer.createDiv({ cls: "game-view-item" });
      const platinumLabel = platinumContainer.createSpan({ cls: "game-view-label" });
      platinumLabel.textContent = this.plugin.t("viewStatusLabel");
      const platinumValue = platinumContainer.createSpan({ cls: "game-view-value" });
      platinumValue.textContent = this.plugin.t("viewStatusPlatinum");
    }
    if (this.canEdit) {
      const buttonContainer = contentEl.createDiv({ cls: "game-view-buttons" });
      const editButton = buttonContainer.createEl("button", { text: this.plugin.t("buttonEdit") });
      editButton.addEventListener("click", () => {
        this.close();
        new AddGameModal(this.app, (editedGame) => {
          this.onEdit(editedGame);
        }, this.plugin, this.game).open();
      });
      const deleteButton = buttonContainer.createEl("button", {
        cls: "game-view-delete-button",
        text: this.plugin.t("buttonDelete")
      });
      deleteButton.addEventListener("click", () => {
        void (async () => {
          const confirmed = await ConfirmActionModal.confirm(
            this.app,
            this.plugin.t("confirmDeleteGame"),
            this.plugin.t("buttonDelete"),
            this.plugin.t("buttonCancel")
          );
          if (!confirmed) {
            return;
          }
          this.close();
          this.onDelete();
        })();
      });
    }
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};
var ConfirmActionModal = class _ConfirmActionModal extends import_obsidian.Modal {
  constructor(app, message, confirmLabel, cancelLabel, onResolve) {
    super(app);
    this.message = message;
    this.confirmLabel = confirmLabel;
    this.cancelLabel = cancelLabel;
    this.onResolve = onResolve;
  }
  static confirm(app, message, confirmLabel, cancelLabel) {
    return new Promise((resolve) => {
      new _ConfirmActionModal(app, message, confirmLabel, cancelLabel, resolve).open();
    });
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.createEl("p", { text: this.message });
    new import_obsidian.Setting(contentEl).addButton((button) => {
      button.setButtonText(this.confirmLabel).setWarning().onClick(() => {
        this.onResolve(true);
        this.close();
      });
    }).addButton((button) => {
      button.setButtonText(this.cancelLabel).onClick(() => {
        this.onResolve(false);
        this.close();
      });
    });
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};
var SteamGridDBSearchModal = class extends import_obsidian.Modal {
  constructor(app, plugin, initialQuery, onSelect) {
    super(app);
    this.games = [];
    this.grids = [];
    this.selectedGame = null;
    this.plugin = plugin;
    this.searchQuery = initialQuery;
    this.onSelect = onSelect;
  }
  onOpen() {
    this.renderSearchView();
    if (this.searchQuery) {
      void this.executeSearch();
    }
  }
  renderSearchView() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.createEl("h2", { text: this.plugin.t("steamGridDbModalTitle") });
    const searchRow = contentEl.createDiv({ cls: "sgdb-search-row" });
    const input = searchRow.createEl("input", { cls: "sgdb-search-input" });
    input.type = "text";
    input.value = this.searchQuery;
    input.placeholder = this.plugin.t("steamGridDbSearchPlaceholder");
    const searchBtn = searchRow.createEl("button", {
      text: this.plugin.t("steamGridDbSearchAction"),
      cls: "mod-cta sgdb-search-btn"
    });
    const resultsContainer = contentEl.createDiv({ cls: "sgdb-results" });
    const runSearch = async () => {
      this.searchQuery = input.value.trim();
      if (!this.searchQuery)
        return;
      resultsContainer.empty();
      resultsContainer.createEl("p", { text: this.plugin.t("steamGridDbLoadingGames") });
      try {
        this.games = await this.plugin.searchSteamGridDB(this.searchQuery);
        this.renderGamesList(resultsContainer);
      } catch (error) {
        resultsContainer.empty();
        const errorMsg = error instanceof Error ? error.message : String(error);
        resultsContainer.createEl("p", { text: this.plugin.t("steamGridDbSearchError", { error: errorMsg }) });
      }
    };
    searchBtn.addEventListener("click", () => {
      void runSearch();
    });
    input.addEventListener("keydown", (e) => {
      if (e.key === "Enter") {
        void runSearch();
      }
    });
    if (this.games.length > 0) {
      this.renderGamesList(resultsContainer);
    }
  }
  async executeSearch() {
    const resultsContainer = this.contentEl.querySelector(".sgdb-results");
    if (!resultsContainer)
      return;
    resultsContainer.empty();
    resultsContainer.createEl("p", { text: this.plugin.t("steamGridDbLoadingGames") });
    try {
      this.games = await this.plugin.searchSteamGridDB(this.searchQuery);
      this.renderGamesList(resultsContainer);
    } catch (error) {
      resultsContainer.empty();
      const errorMsg = error instanceof Error ? error.message : String(error);
      resultsContainer.createEl("p", { text: this.plugin.t("steamGridDbSearchError", { error: errorMsg }) });
    }
  }
  renderGamesList(container) {
    container.empty();
    if (this.games.length === 0) {
      container.createEl("p", { text: this.plugin.t("steamGridDbNoResults") });
      return;
    }
    const list = container.createEl("ul", { cls: "sgdb-games-list" });
    this.games.forEach((game) => {
      const item = list.createEl("li", { text: game.name, cls: "sgdb-game-item" });
      item.addEventListener("click", () => {
        this.selectedGame = game;
        void this.loadGridsForGame(game);
      });
    });
  }
  async loadGridsForGame(game) {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.createEl("h2", { text: this.plugin.t("steamGridDbModalTitle") });
    contentEl.createDiv({ cls: "sgdb-subtitle", text: game.name });
    const loadingEl = contentEl.createEl("p", { text: this.plugin.t("steamGridDbLoadingGrids") });
    try {
      this.grids = await this.plugin.getSteamGridDBGrids(game.id);
      loadingEl.remove();
      this.renderGridView();
    } catch (error) {
      loadingEl.remove();
      const errorMsg = error instanceof Error ? error.message : String(error);
      contentEl.createEl("p", { text: this.plugin.t("steamGridDbSearchError", { error: errorMsg }) });
      const backBtn = contentEl.createEl("button", { text: this.plugin.t("steamGridDbBackButton"), cls: "sgdb-back-btn" });
      backBtn.addEventListener("click", () => {
        this.renderSearchView();
      });
    }
  }
  renderGridView() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.createEl("h2", { text: this.plugin.t("steamGridDbModalTitle") });
    const backBtn = contentEl.createEl("button", {
      text: this.plugin.t("steamGridDbBackButton"),
      cls: "sgdb-back-btn"
    });
    backBtn.addEventListener("click", () => {
      this.renderSearchView();
    });
    if (this.selectedGame) {
      contentEl.createDiv({ cls: "sgdb-subtitle", text: this.selectedGame.name });
    }
    if (this.grids.length === 0) {
      contentEl.createEl("p", { text: this.plugin.t("steamGridDbNoGrids") });
      return;
    }
    const grid = contentEl.createDiv({ cls: "sgdb-grids-container" });
    this.grids.forEach((gridItem) => {
      const wrapper = grid.createDiv({ cls: "sgdb-grid-item" });
      const img = wrapper.createEl("img", { cls: "sgdb-grid-thumb" });
      img.src = gridItem.thumb || gridItem.url;
      img.alt = "";
      img.addEventListener("click", () => {
        this.onSelect(gridItem.url);
        this.close();
      });
    });
  }
  onClose() {
    this.contentEl.empty();
  }
};
var GameBacklogSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsTitle")).setHeading();
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsLanguageName")).setDesc(this.plugin.t("settingsLanguageDesc")).addDropdown((dropdown) => dropdown.addOption("es", this.plugin.t("languageSpanish")).addOption("en", this.plugin.t("languageEnglish")).setValue(this.plugin.settings.language).onChange(async (value) => {
      this.plugin.settings.language = value;
      await this.plugin.saveSettings();
      this.display();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsDefaultCoverName")).setDesc(this.plugin.t("settingsDefaultCoverDesc")).addText((text) => text.setPlaceholder(this.plugin.t("settingsDefaultCoverPlaceholder")).setValue(this.plugin.settings.defaultCoverImage).onChange(async (value) => {
      this.plugin.settings.defaultCoverImage = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsDefaultPlatformName")).setDesc(this.plugin.t("settingsDefaultPlatformDesc")).addDropdown((dropdown) => {
      const selectEl = dropdown.selectEl;
      PLATFORM_GROUPS.forEach((group) => {
        const groupHeader = document.createElement("option");
        groupHeader.textContent = `--- ${group.label} ---`;
        groupHeader.disabled = true;
        groupHeader.value = "";
        selectEl.appendChild(groupHeader);
        group.platforms.forEach((platform) => {
          dropdown.addOption(platform, platform);
        });
      });
      dropdown.setValue(this.plugin.settings.defaultPlatform).onChange(async (value) => {
        this.plugin.settings.defaultPlatform = value;
        await this.plugin.saveSettings();
      });
    });
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsPlatformModeName")).setDesc(this.plugin.t("settingsPlatformModeDesc")).addDropdown((dropdown) => dropdown.addOption("none", this.plugin.t("platformModeNone")).addOption("label", this.plugin.t("platformModeLabel")).setValue(this.plugin.settings.platformMode).onChange(async (value) => {
      this.plugin.settings.platformMode = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsScoreTypeName")).setDesc(this.plugin.t("settingsScoreTypeDesc")).addDropdown((dropdown) => dropdown.addOption("stars-5", this.plugin.t("scoreTypeStars5")).addOption("stars-5-half", this.plugin.t("scoreTypeStars5Half")).addOption("stars-10", this.plugin.t("scoreTypeStars10")).addOption("numeric-10", this.plugin.t("scoreTypeNumeric10")).setValue(this.plugin.settings.scoreType).onChange(async (value) => {
      this.plugin.settings.scoreType = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsDownloadFolderName")).setDesc(this.plugin.t("settingsDownloadFolderDesc")).addText((text) => text.setPlaceholder(this.plugin.t("settingsDownloadFolderPlaceholder")).setValue(this.plugin.settings.imageDownloadFolder).onChange(async (value) => {
      this.plugin.settings.imageDownloadFolder = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsSteamGridDbName")).setDesc(this.plugin.t("settingsSteamGridDbDesc")).addText((text) => text.setPlaceholder(this.plugin.t("settingsSteamGridDbPlaceholder")).setValue(this.plugin.settings.steamGridDbApiKey).onChange(async (value) => {
      this.plugin.settings.steamGridDbApiKey = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsCardColorName")).setDesc(this.plugin.t("settingsCardColorDesc")).addText((text) => text.setPlaceholder(this.plugin.t("settingsCardColorPlaceholder")).setValue(this.plugin.settings.cardColor).onChange(async (value) => {
      this.plugin.settings.cardColor = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsTextColorName")).setDesc(this.plugin.t("settingsTextColorDesc")).addText((text) => text.setPlaceholder(this.plugin.t("settingsTextColorPlaceholder")).setValue(this.plugin.settings.textColor).onChange(async (value) => {
      this.plugin.settings.textColor = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsNoImageBackgroundColorName")).setDesc(this.plugin.t("settingsNoImageBackgroundColorDesc")).addText((text) => text.setPlaceholder(this.plugin.t("settingsNoImageBackgroundColorPlaceholder")).setValue(this.plugin.settings.noImageBackgroundColor).onChange(async (value) => {
      this.plugin.settings.noImageBackgroundColor = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsNoImageTextColorName")).setDesc(this.plugin.t("settingsNoImageTextColorDesc")).addText((text) => text.setPlaceholder(this.plugin.t("settingsNoImageTextColorPlaceholder")).setValue(this.plugin.settings.noImageTextColor).onChange(async (value) => {
      this.plugin.settings.noImageTextColor = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian.Setting(containerEl).setName(this.plugin.t("settingsPluginAssetsName")).setDesc(this.plugin.t("settingsPluginAssetsDesc")).addButton((button) => {
      button.setButtonText(this.plugin.t("settingsPluginAssetsButton")).onClick(async () => {
        button.setDisabled(true);
        button.setButtonText(this.plugin.t("settingsPluginAssetsRunning"));
        try {
          await this.plugin.downloadPluginAssetsFromRepo();
        } finally {
          button.setDisabled(false);
          button.setButtonText(this.plugin.t("settingsPluginAssetsButton"));
        }
      });
    });
  }
};

/* nosourcemap */